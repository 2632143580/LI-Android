把 LI 项目构建出的单文件 dist/index.html 复制到这里，并重命名为 index.html。

步骤：
1. 在 LI 项目执行 启动LI-构建.bat（或 npm run build）得到 dist/index.html
2. 把 dist/index.html 放到本目录，文件名保持 index.html
3. 重新构建 APK 即可

这样 LI 完全离线运行在手机里，不依赖任何服务器。
