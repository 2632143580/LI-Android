# 良好设计.md —— 设置页/配置类功能的行业标准做法

> 来源：2026-08-18 用户反馈复盘（服务商列表做错、刷新按钮做错、Key 无眼睛、URL/baseUrl 没说清、语音 Key 不同步）。
> 原则：**做功能前先想行业怎么做的；能复用 LI 网页已有实现就复用，绝不自己发明过时方案。**

## 1. LLM 配置：服务商 → 拉取模型列表 → 选模型（行业标准，禁止预填模型名）

- **正确**（OpenAI 兼容生态标准，LI 网页已实现，`src/ui/event-bindings/settings.js`）：
  1. 服务商按钮（智谱/DeepSeek/…）→ 点选只填**接口地址**（chat/completions 完整端点）
  2. 用户填 API Key
  3. 点「拉取模型列表」→ `GET {地址去 /chat/completions → /models}`，头 `Authorization: Bearer <key>` → `data[].id || data[].name`
  4. 模型列表点选 → 填入模型名
- **错误**（本次犯的）：预设列表里写死模型名（glm-4-flash/deepseek-chat…）——**模型名会过时**，服务商会变；任何写死模型名的"预设"都是劣质设计。
- 地址字段：**完整 chat/completions 端点**（含 `/v1/chat/completions`），不是 baseUrl。界面必须写明"这是 chat/completions 完整端点"。

## 2. Key 输入：必须有「显示/隐藏」眼睛（默认操作）

- 掩码是默认，但**必须能一键切明文**（行业常识，LI 网页 LLM Key 和语音 Key 都有）。
- App 侧已做：`btnToggleKey`/`btnToggleTtsKey` 切换 `inputType` password ↔ visible_password。

## 3. 语音（MiMo TTS）配置

- Key 填了必须同步进网页：注入脚本 `settings.ttsSource='cloud'` + `settings.ttsCloud={apiKey,baseUrl,model}`（结构见 `LI/src/core/constants.js` 注释）。
- **地址要显示完整**：`https://api.xiaomimimo.com/v1`，不能只写"xiaomimimo"。
- 语音地址是 **baseUrl**（不含 /chat/completions），和 LLM 的完整端点不同——**界面必须区分清楚**。

## 4. 不覆盖用户已有配置（铁律）

- App 侧未显式填写的字段（地址/模型）**绝不写入网页**——默认值（如 openai/mimo 默认 URL）会覆盖用户网页里已有的配置。
- 实现：AppPreferences 默认值存**空串**（=未设置）；注入 JS 只在 `NATIVE.xx` 非空时覆盖；LlmClient 请求时才 fallback 默认。

## 5. 同步注入：写入永不丢弃，只有 reload 有上限

- 反模式（本次 bug 根因）：防闪烁的 `liReloads>=2` 一刀切 `return` → 连 **localStorage 写入**都被丢弃 → 语音 Key 永远进不去网页。
- 正确：**写入永远执行**（幂等比对后才写，持久化一定成功）；`location.reload()` 才有上限（防闪烁死循环）；达到上限时配置已入库，下次打开即生效。

## 6. 「刷新」按钮语义：一个功能一个刷新，放在它刷新的对象旁

- 主界面「刷新」= 刷新 LI 网页（重载 WebView）——用户认知里刷新只有这一个，位置在主界面设置按钮旁边。
- 设置页统计不设独立刷新按钮：保存后自动重取即可。
- 教训：别把"统计刷新"和"网页刷新"混为一谈；用户说"刷新"时先确认指哪个。

## 7. 做功能前先查项目已有实现

- LI 网页项目（src/）已实现大量标准交互：服务商标签、模型拉取、Key 眼睛、语音配置面板。**App 设置页设计先读这些代码对齐，不重复发明**。
- 数据/契约单一事实源：`LI/src/core/constants.js`（settings 结构）、`src/core/utils.js`（getProviderByUrl）、`index.html`（provider-tab 的 data-url）。

## 8. 避免"下载-安装-卸载-反馈"恶性循环

- 每轮改动做完整自查再出包：R.id 全量核对、import 补全、JS 逻辑因果闭环、对照 LI 现有实现。
- 不确定的设计点（交互方式、字段语义）**先问/先说，不替用户做想当然的视觉或交互决定**。
